[variables]
    NIXPACKS_PHP_VERSION = '8.4'
    NIXPACKS_PHP_EXTENSIONS = 'gd, pdo_mysql, zip, opcache, bcmath'

[phases.install]
    dependsOn = ['setup']
    cmds = ['chmod -R 775 storage bootstrap/cache']
